后端必须改两点：

1. 接收 Android 发来的 deadline_sec

// GameMetaSyncReq — 加字段
type GameMetaSyncReq struct {
// ... 已有字段 ...
DurationSec  int64  `json:"duration_sec"`
DeadlineSec  int64  `json:"deadline_sec"`    // ← 新增！Android 现在会传
}

// ChainStateSyncReq — 加字段
type ChainStateSyncReq struct {
// ... 已有字段 ...
DeadlineSec  int64  `json:"deadline_sec"`    // ← 新增！Android 现在会传
}

2. 修复默认值（核心 BUG）

// ❌ 当前的做法（推测）：
if req.DeadlineSec == 0 {
req.DeadlineSec = time.Now().UnixMilli()  // BUG! 毫秒写进了秒字段!
}

// ✅ 修复后：直接用 Android 传来的值，不给默认值
// Android 现在已经正确传了 deadline_sec，不需要后端自己算
// 如果确实需要兜底，也应该用秒：
if req.DeadlineSec == 0 {
req.DeadlineSec = time.Now().Unix() + req.DurationSec  // 秒 + 秒 = 秒
}

Android 端的改动我全都做好了，现在跑一下就能看到 GameMetaSyncReq 和 ChainStateSyncReq 请求 body 里都带了 deadline_sec 字段。你只需要在后端 Go
代码里接收并存储这个字段，不要再自己用 UnixMilli() 填默认值就行。